# Seeds de Firestore

Edita `vocabulario.json` para agregar palabras. Las categorías se convierten en unidades y ejercicios automáticamente al ejecutar `npm run seed:firestore`.

Los valores `targetText` del ejemplo están en español para que la app sea comprobable. Reemplázalos por la lengua indígena antes de publicar el contenido.
